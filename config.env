# ============================================================================
# Configuración centralizada para nginx-k3s
# ============================================================================

# Namespace donde se desplegará nginx
NAMESPACE="${NAMESPACE:-nginx-namespace}"

# Dominio para el certificado SSL y el Ingress
DOMAIN="${DOMAIN:-nginx.negociapp.com}"

# Email para Let's Encrypt (notificaciones de certificados)
EMAIL="${EMAIL:-andresgarcia0313@gmail.com}"

# Configuración del despliegue
REPLICAS="${REPLICAS:-3}"

# Timeouts (en segundos)
DEPLOYMENT_TIMEOUT="${DEPLOYMENT_TIMEOUT:-300}"
CERTIFICATE_TIMEOUT="${CERTIFICATE_TIMEOUT:-600}"

# Servidor ACME de Let's Encrypt
# Usar "production" para certificados reales o "staging" para pruebas
ACME_SERVER="${ACME_SERVER:-production}"

# Imágenes de contenedores
NGINX_IMAGE="${NGINX_IMAGE:-nginx:latest}"

# Recursos del pod (CPU y Memoria)
CPU_REQUEST="${CPU_REQUEST:-50m}"
CPU_LIMIT="${CPU_LIMIT:-200m}"
MEMORY_REQUEST="${MEMORY_REQUEST:-64Mi}"
MEMORY_LIMIT="${MEMORY_LIMIT:-128Mi}"
